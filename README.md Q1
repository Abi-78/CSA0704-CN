Parity is a simple error detection method that checks whether the number of 1s in binary data is even or odd.

Checksum is calculated by adding the ASCII values of all characters in the message. The receiver compares the checksum to detect errors.

In this project, both parity and checksum were used to detect transmission errors. Checksum can detect more types of errors than a simple parity bit.
